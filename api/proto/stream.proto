syntax = "proto3";
package v_stream;

option go_package = "github.com/Be3751/v-stream/pkg/pb";

service VideoStream {
	rpc ReceiveVideo (SendRequest) returns (stream SendResponse) {};
}

message SendRequest {
	string videoId = 1;
}

message SendResponse {
	string name = 1;
	bytes video = 2;
}
